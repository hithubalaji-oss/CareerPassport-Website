---
/**
 * For Companies - hiring managers and talent teams.
 *
 * Reworked in the latest design pass. The page now opens on a PINNED hero where the brief
 * is written: a Companion composer types the hiring intent out as you scroll, with the
 * partner-logo strip absorbed into the same fold. The standalone logo strip and the
 * boarding-pass testimonials are gone.
 *
 * Everything after the hero remains ordinary blocks in the document flow, except the
 * process stage. That was a deliberate correction: an earlier build pinned every fold and
 * computed per-fold bands, and headings slid under the header while panels disagreed
 * about their available space.
 *
 * One grid governs the page (declared at the top of companies.css):
 *   --gut 20-72px gutters - --measure 1296px - --copy 664px - --sec 96-152px rhythm
 */
import Base from '../layouts/Base.astro';
import Header from '../components/chrome/Header.astro';
import Footer from '../components/chrome/Footer.astro';
import Ground from '../components/companies/Ground.astro';
import Hero from '../components/companies/Hero.astro';
import ProcessDemo from '../components/companies/ProcessDemo.astro';
import Comparison from '../components/companies/Comparison.astro';
import Closing from '../components/companies/Closing.astro';

import '../styles/companies.css';
---

<Base
  title="CareerPassport — For Companies"
  description="Tell us who you need. CareerPassport builds the hiring journey, runs it, and brings you the evidence — so you meet fewer people and know more about the ones you do."
>
  <Ground />
  <Header />

  <Hero />
  <ProcessDemo />
  <Comparison />
  <Closing />

  <Footer />

  <script src="/image-slot.js" is:inline></script>
  <script src="../scripts/companies.js"></script>
</Base>
