{
  "name": "careerpassport-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing site for CareerPassport \u2014 a verified-career-record product.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "npm run check:design && npm run check:deltas",
    "check:deltas": "node scripts/check-deltas.mjs",
    "check:design": "node scripts/check-design-files.mjs"
  },
  "devDependencies": {
    "astro": "^5.6.1"
  }
}
